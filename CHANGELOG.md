## 0.1.0

- Initial workspace package.
- Re-export `package:barcode` and `package:pretty_qr_code`.
- Add `PrettyBarcodeView` for Flutter rendering of all barcode formats.
- Render QR codes through `PrettyQrView` by default.
- Add `PrettyBarcodeOptions` and `PrettyBarcodeQrOptions`.
- Add `PrettyBarcodeSvg` for SVG export.
